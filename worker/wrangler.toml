name = "atc-pdf-drive"
main = "src/index.js"
compatibility_date = "2025-01-01"

# Secrets are set via: wrangler secret put <NAME>
# Required secrets:
#   SA_EMAIL      — service account email (e.g. tl-helper@tl-helper-495507.iam.gserviceaccount.com)
#   SA_PRIVATE_KEY — service account private key (full PEM, including header/footer lines)
#   FOLDER_ID     — Google Drive folder ID from the share URL
